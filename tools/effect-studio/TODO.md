# Effect Studio TODO

- runtime import/export を JSON file 経由でつなぐ
- track parameter を scalar curve ではなく keyframe editor 契約に広げる
- burst だけでなく sustained emitter / beam / mesh effect を追加する
- AI patch loop 用の script 群を `tools/effect-studio/scripts` に追加する
- timeline 上の track grouping / nesting を追加する

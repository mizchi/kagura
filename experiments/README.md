# Experimental implementations

公開 module の一部ではない研究・比較用実装。`webgpu/` は旧 `platform_web/gfx_webgpu`。
リポジトリ内の利用側 import がなく、現行の `assets/web/kagura-gfx.js` とは機能が異なる。
削除せず履歴・テストを保つが、現行 platform の実装として配布しない。
`moon test experiments/webgpu --target js` で確認する。

{
  "apiVersion": 1,
  "id": "demo.wasm",
  "title": "Wasm tools",
  "tools": [{
    "name": "inspect_request",
    "description": "Inspect the UTF-8 request and current scene delivered to the Wasm plugin",
    "effect": "read",
    "inputSchema": { "type": "object", "properties": { "value": {} }, "required": ["value"], "additionalProperties": false },
    "outputSchema": { "type": "object" }
  }]
}

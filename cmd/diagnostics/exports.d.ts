/** JSON boundary: {ok:true,value:unknown} or {ok:false,error:string}. */
export function analyzePerformance(operation: 'numeric'|'metrics'|'browser'|'trace'|'cpu'|'perFrame', source: string): string;
